<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
    <title>Blog</title>
</head>
<body>
    <div class="container">
        <h1 class="mb-4">Data History</h1>

        <% records.forEach(record => { %>
            <div class="card mt-4">
                <div class="card-body">
                    <h4 class="card-title"><%= record.Car_ID %></h4>
                    <div class="card-subtitle text-muted mb-2">
                        <%= record.Booking_ID %>
                    </div>
                    <div class="card-subtitle text-muted mb-2">
                        <%= record._id %>
                    </div>
                    <div class="card-text mb-2"> <%= record.Collision_Data %> </div>
                    <div class="card-text mb-2"> <%= record.Invasion_Data %> </div>
                    <div class="card-text mb-2"> <%= record.GNSS_Data %> </div>

                    <a href="records/<%= record._id %>" class="btn btn-primary">Read More</a> 

                    <form action="/records/<%= record.id %>?_method=DELETE" method="POST" class="d-inline">
                        <button type="submit" class="btn btn-danger">Delete</button>
                    </form>
                </div>
            </div>
        <% }) %>
    </div>
</body>
</html>